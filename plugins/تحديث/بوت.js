export default async function before(m, { conn }) {
    // التأكد إن الرسالة فيها نص وتحويله لنص عادي بدون مسافات
    const text = m.text?.trim();

    // الشرط: البوت يلقط كلمة "بوت" من أي حد
    if (text === "بوت" || text === "يا بوت") {
        
        // الرد المزخرف اللي طلبته
        const kingReply = `*⌞ 𝑲𝑰𝑵𝑮 𝑩𝑶𝑻 🃏 ⌝*

*الـكـيـنـج فـي الـخـدمـة يـا بـطـل ✨*
*تـأمـرنـي بـأي شـيء ؟ 🫡🔥*

*⌞ 𝑲𝑰𝑵𝑮 𝑩𝑶𝑻🃏 ⌝*`;

        // إرسال الرد مع عمل منشن أو رد (quoted) على الشخص
        return m.reply(kingReply);
    }

    // السماح لبقية الأوامر بالعمل بشكل طبيعي
    return false;
}